# fprime-stress-reference Python requirements.
#
# Pulls in the F Prime framework's own requirements (fprime-tools,
# fprime-gds, fprime-fpp, ...), then layers on the DOOM-fetch CLI
# that ships inside the fprime-stress library and the YAMCS launcher.
-r ./lib/fprime/requirements.txt
./lib/fprime-stress/tools/fprime-get-doom
./tools/fprime-doom
# fprime-yamcs 0.1.7 ships the web-extension / realtime-only-channel
# support and the prebuilt plugin jar (source checkouts require Maven).
fprime-yamcs==0.1.7
